<h1>Look at these beautiful plants</h1>

<ul>
  <% @plants.each do |plant| %>
  <li><%= link_to plant.name, plant_path(plant) %> - <%= plant.description %>, <%= plant.location %> / <%= plant.price %>$ <%= plant.category %></li>
  <% end %>
</ul>

